name: smart_medicine_box
description: Akıllı İlaç Kutusu - IoT Projesi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^4.3.0
  firebase_database: ^12.1.1
  firebase_auth: ^6.1.3
  firebase_messaging: ^16.1.0
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
